<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yeseul.memorycard">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PRECISE_PHONE_STATE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mc_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.yeseul.memorycard.presentation.wordlist.AddWordActivity"
            android:exported="true" />
        <activity
            android:name="com.yeseul.memorycard.presentation.wordcard.WordCardActivity"
            android:exported="true" />

        <activity
            android:name="com.yeseul.memorycard.presentation.wordlist.WordListActivity"
            android:theme="@style/SplashTheme"

            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">

        </activity>

    </application>

</manifest>